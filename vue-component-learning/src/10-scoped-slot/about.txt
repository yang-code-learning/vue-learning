编译作用域：

作用域插槽：希望能访问到子组件的内容，
如传入列表，子组件逐个遍历，父组件插入slot的东西要获得每次遍历的数据
语法有点不理解，v-slot=""

独占默认插槽：当slot是对应名字是默认default的，
且子组件中只有只有这么设计了一个插槽（欸，只有一个了不就是默认名字么），
就可以把v-slot=""写道子组件属性中，否则里面要写<template>区分不同slot，
感觉用处不是很大，可读性更强吧